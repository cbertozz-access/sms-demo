# Basic Auth credentials
AUTH_USER=demo
AUTH_PASSWORD=your-secure-password

# Builder.io API Key (get from Builder.io → Settings → API)
NEXT_PUBLIC_BUILDER_API_KEY=your-builder-api-key

# Iterable API Key (get from Iterable → Integrations → API Keys)
ITERABLE_API_KEY=your-iterable-api-key
